>git init[to initialize git]
>git status[to list all untracked files]
>git add-A[to adding all untracked files to staging area] 
>git commit -m "commit message"[to commit added filke to local respository]
>git clone[to take take exact copy from remote respository to local workspace]
>git pull[to take changes from remote respositoryto local workspace]
>git push[to sent changes from local respository to remote respository]